		<attachment name="conditionAttachment-${player}-AlwaysTrue" attachTo="${player}" javaClass="games.strategy.triplea.attachments.RulesAttachment" type="player">
			<option name="techs" value="mechanizedInfantry" count="1" />
		</attachment>
		<!-- Remove after battle to avoid "this unit cannot land" warnings -->
		<attachment name="triggerAttachment_Remove_${player}_${unit}_1" attachTo="${player}" javaClass="games.strategy.triplea.attachments.TriggerAttachment" type="player">
			<option name="conditions" value="conditionAttachment-${player}-AlwaysTrue" />
			<option name="when" value="after:${player}Battle" />
			<option name="removeUnits" value="all:${unit}" count="9999" />
		</attachment>
		<!-- Remove after end turn since we create the actual ones at the start of the next turn -->
		<attachment name="triggerAttachment_Remove_${player}_${unit}_2" attachTo="${player}" javaClass="games.strategy.triplea.attachments.TriggerAttachment" type="player">
			<option name="conditions" value="conditionAttachment-${player}-AlwaysTrue" />
			<option name="when" value="after:${player}EndTurn" />
			<option name="removeUnits" value="all:${unit}" count="9999" />
		</attachment>